# PrimeLayer Admin Panel

Flutter web admin panel. Feature-first clean architecture.

## Run

```bash
flutter run -d chrome
```

## Layout

- `lib/core/` — router, theme, fonts, shared utils
- `lib/features/` — one folder per feature (`data` / `domain` / `presentation`)
