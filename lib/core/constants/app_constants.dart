abstract final class AppConstants {
  static const String appName = 'PrimeLayer Admin Panel';
  static const String studioName = 'Prime Layer Studio';
  static const Duration splashDuration = Duration(milliseconds: 2200);
  static const Duration signInLoaderDuration = Duration(seconds: 2);
}
